<div class="flex cursor-pointer border px-4 py-2 text-lg text-grey-darkest">
    <input type="radio" id="{{ $value }}" name="categoria" value="{{ $value }}">
    <label for="{{ $value }}" class="ml-4">{{ $label }}</label>
</div>
